# Optional: docker compose up works without this file.
WEB_PORT=3000
API_PORT=3001
# Use URL-safe characters; this local demo password is not a production secret.
POSTGRES_PASSWORD=local-demo-password
GENERATOR_INTERVAL_MS=650
